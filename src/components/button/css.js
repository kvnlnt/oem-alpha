(function(COMPONENTS, THEME) {

    var Css = [
        THEME.BUTTON,
        THEME.BUTTON_HOLLOW
    ];

    COMPONENTS.Button.Css = Css;
    return COMPONENTS;

})(oem.Components, oem.Components.Theme);