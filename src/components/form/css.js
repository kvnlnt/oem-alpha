(function(COMPONENTS, THEME) {

    var Css = [{
        selector: " fieldset",
        declaration: {
            margin:"0 0 10px 0",
            padding:0,
            border:0
        }
    }];
    COMPONENTS.Form.Css = Css;
    return COMPONENTS;

})(oem.Components, oem.Components.Theme);