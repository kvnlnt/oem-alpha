ToolhouseUI.Components = (function(Components, Core) {

    var COMPONENT_CLASSView = {};
    COMPONENT_CLASSView.id = 'COMPONENT_NAME-css';
    COMPONENT_CLASSView.css = [

        {
            selector: "COMPONENT_NAME, .COMPONENT_NAME",
            declaration: [
            ]
        }

    ];

    // add to css renderer
    Core.Css.add({
        id: COMPONENT_CLASSView.id,
        css: COMPONENT_CLASSView.css
    });

    Components.CardView = COMPONENT_CLASSView;
    return Components;

})(ToolhouseUI.Components || {}, ToolhouseUI.Core);